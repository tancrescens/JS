let prompt = require("prompt-sync")();

// Input
// Process
// Output

x = 1;
y = 2;

function addTwoNumbers(x, y) {
  let total = x + y;
  return total;
}

let result = addTwoNumbers(100, 200);
console.log(result);
